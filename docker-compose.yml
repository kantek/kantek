version: '3.8'

services:
    db:
        image: postgres
        restart: always
        ports:
            - 5432:5432
        environment:
            - POSTGRES_USER=kantek
            - POSTGRES_DB=kantek
            - POSTGRES_PASSWORD=kantek
        volumes:
            - db:/var/lib/postgresql/data

volumes:
    db:
